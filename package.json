{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.5.5"
  },
  "dependencies": {
    "@storybook/storybook-deployer": "^2.8.16"
  }
}
